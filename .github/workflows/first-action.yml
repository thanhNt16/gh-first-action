name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: |
          echo "Hello world"
          echo "Hello multiple world"
      - name: Print goodbye
        run: echo "Goodbye"
